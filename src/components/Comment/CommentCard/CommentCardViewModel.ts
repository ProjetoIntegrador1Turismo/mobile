import { CommentCardProps } from '~/src/components/Comment/CommentCard/CommentCard.types';

export const useCommentCardViewModel = () => {
  const handleOptionsPress = () => {
    // Add business logic for options menu
  };

  return {
    handleOptionsPress,
  };
};
